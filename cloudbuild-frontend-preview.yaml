steps:
  # Build the frontend Docker image for preview
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-f'
      - 'apps/frontend/Dockerfile'
      - '-t'
      - 'gcr.io/$PROJECT_ID/frontend-preview'
      - '.'

  # Push the image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/frontend-preview'

  # Deploy to Cloud Run
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'krakengaming-frontend-preview'
      - '--image'
      - 'gcr.io/$PROJECT_ID/frontend-preview'
      - '--region'
      - 'us-central1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--set-env-vars'
      - 'NEXT_PUBLIC_ENVIRONMENT=preview,NEXTAUTH_URL=https://preview.krakengaming.org'
      - '--set-secrets'
      - 'AUTH_DISCORD_ID=auth-discord-id:latest,AUTH_DISCORD_SECRET=auth-discord-secret:latest,AUTH_SECRET=auth-secret:latest,AUTH_URL=auth-url-preview:latest,DATABASE_URL=database-url:latest'

images:
  - 'gcr.io/$PROJECT_ID/frontend-preview'

options:
  logging: CLOUD_LOGGING_ONLY
